[project]
name = "postgisstudio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "asyncpg",
    "jinja2",
    "python-dotenv",
    "shapely",
]
